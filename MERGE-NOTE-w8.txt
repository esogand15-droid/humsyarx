🌊 W8 — راهنمای ادغام (مهم)

فایل‌های جدید (مستقیم کپی شوند):
  miniapp/src/pages/Ai/AiImage.jsx
  tests/test_w8_image_gen.py

۹ فایل مشترک تغییریافته هم با مسیر اصلی داخل ZIP هستند، اما اگر ریپوی
شما موج‌های W6/اصلاح-آپلود/W7 را مرج‌شده دارد، آن‌ها را کورکورانه
overwrite نکنید؛ به‌جایش پچ را اعمال کنید:

  git apply --3way w8-image-gen.patch
  (یا: patch -p1 < w8-image-gen.patch)

پچ فقط تغییرات W8 است و تست شده: اعمالش روی درخت pre-W8، فایل‌های
نهایی را دقیقاً بازتولید می‌کند.

فایل w8-image-gen.patch و همین MERGE-NOTE جزو ریپو نیستند؛ بعد از
ادغام حذفشان کنید.
